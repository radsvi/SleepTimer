<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:SleepTimer.Models"
             xmlns:viewModels="clr-namespace:SleepTimer.ViewModels"
             x:Class="SleepTimer.Views.SleepLogsPage"
             x:DataType="viewModels:SleepLogsVM"
             Title="Sleep Logs">
    <AbsoluteLayout Margin="5">
        <VerticalStackLayout
            AbsoluteLayout.LayoutBounds="0,0,1,300"
            AbsoluteLayout.LayoutFlags="WidthProportional"
            >
            <CollectionView SelectionMode="None" ItemsSource="{Binding AppPreferences.LogEntries}" >
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:LogEntry">
                        <HorizontalStackLayout>
                            <Label Text="{Binding Date}" Margin="0,0,3,0" />
                            <Label Text="{Binding Text}" />
                        </HorizontalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>

        <Grid
            AbsoluteLayout.LayoutBounds="0,1,1,0.06"
            AbsoluteLayout.LayoutFlags="All">
            <Button Text="Clear Logs" Command="{Binding LogsHandler.ClearCommand}" />
        </Grid>
        
    </AbsoluteLayout>
</ContentPage>